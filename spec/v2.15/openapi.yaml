# Copyright 2026 AgentMindCloud
# Licensed under the Apache License, Version 2.0
# http://www.apache.org/licenses/LICENSE-2.0
#
# Auto-generated by scripts/export-openapi.py from cli/grok-agent.py.
# Do NOT edit by hand — the next regeneration will overwrite changes.
# Spec version: v2.15. Accepted versions: 2.14, 2.15.
# Built for xAI, X, Grok and the ecosystem community. ❤️

openapi: 3.1.0
info:
  title: Grok Agent OS Manifest API
  version: '2.15'
  description: OpenAPI 3.1 envelope around the Grok Agent OS v2.15 manifest schema.
    The notional /validate endpoint is documentation-only — no server is required.
    Any OpenAPI-aware client can read components.schemas.GrokAgentManifest to validate
    a manifest locally. Built for xAI, X, Grok and the ecosystem community.
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
  contact:
    name: AgentMindCloud
    url: https://github.com/AgentMindCloud/grok-agent
externalDocs:
  description: Grok Agent OS documentation + spec
  url: https://github.com/AgentMindCloud/grok-agent
paths:
  /validate:
    post:
      summary: Validate a v2.15 manifest
      description: Notional endpoint. The manifest in the request body is validated
        against components.schemas.GrokAgentManifest. No server is required — this
        is for OpenAPI tooling only.
      operationId: validateManifest
      requestBody:
        required: true
        content:
          application/yaml:
            schema:
              $ref: '#/components/schemas/GrokAgentManifest'
          application/json:
            schema:
              $ref: '#/components/schemas/GrokAgentManifest'
      responses:
        '200':
          description: Valid manifest
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  version:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
        '422':
          description: Manifest failed v2.15 schema validation
components:
  schemas:
    GrokAgentManifest:
      additionalProperties: true
      properties:
        schema_meta:
          anyOf:
          - $ref: '#/$defs/SchemaMeta'
          - type: 'null'
          default: null
        version:
          title: Version
          type: string
        kind:
          enum:
          - agent
          - finance-dashboard
          - alpha-engine
          - creator-payout-optimizer
          - vision-analyzer
          - super-agent
          - x-native
          - creator-template
          title: Kind
          type: string
        name:
          pattern: ^[a-z][a-z0-9-]*$
          title: Name
          type: string
        description:
          minLength: 10
          title: Description
          type: string
        author:
          minLength: 1
          title: Author
          type: string
        license:
          const: Apache-2.0
          title: License
          type: string
        metadata:
          anyOf:
          - $ref: '#/$defs/Metadata'
          - type: 'null'
          default: null
        install:
          anyOf:
          - $ref: '#/$defs/Install'
          - type: 'null'
          default: null
        windows:
          anyOf:
          - $ref: '#/$defs/Windows'
          - type: 'null'
          default: null
        grok:
          anyOf:
          - $ref: '#/$defs/Grok'
          - type: 'null'
          default: null
        tools:
          items:
            $ref: '#/$defs/Tool'
          title: Tools
          type: array
        public_apis:
          items:
            $ref: '#/$defs/PublicApi'
          title: Public Apis
          type: array
        multi_agent:
          anyOf:
          - $ref: '#/$defs/MultiAgent'
          - type: 'null'
          default: null
        real_time_x:
          anyOf:
          - $ref: '#/$defs/RealTimeX'
          - type: 'null'
          default: null
        memory:
          anyOf:
          - $ref: '#/$defs/Memory'
          - type: 'null'
          default: null
        provenance:
          anyOf:
          - $ref: '#/$defs/Provenance'
          - type: 'null'
          default: null
        constitution:
          anyOf:
          - $ref: '#/$defs/Constitution'
          - type: 'null'
          default: null
        safety:
          anyOf:
          - $ref: '#/$defs/Safety'
          - type: 'null'
          default: null
        dependencies:
          anyOf:
          - $ref: '#/$defs/Dependencies'
          - type: 'null'
          default: null
        evaluation:
          anyOf:
          - $ref: '#/$defs/Evaluation'
          - type: 'null'
          default: null
      required:
      - version
      - kind
      - name
      - description
      - author
      - license
      type: object
      x-license: Apache-2.0
      x-spec-version: '2.15'
      x-accepted-versions:
      - '2.14'
      - '2.15'
      x-source: https://github.com/AgentMindCloud/grok-agent/blob/main/cli/grok-agent.py
      x-note: JSON does not support comments. License + provenance metadata are carried
        in the $schema and info blocks below; the canonical license is Apache-2.0,
        the canonical source is https://github.com/AgentMindCloud/grok-agent/blob/main/spec/v2.15/openapi.yaml
x-spec-version: '2.15'
x-accepted-versions:
- '2.14'
- '2.15'
x-license: Apache-2.0
