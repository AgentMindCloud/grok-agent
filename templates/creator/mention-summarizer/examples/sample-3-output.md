<!-- Copyright 2026 AgentMindCloud -->
<!-- Licensed under the Apache License, Version 2.0 -->
<!-- http://www.apache.org/licenses/LICENSE-2.0 -->
<!-- Sample 3 — 7-day window case for the Mention Summarizer (Mention Audit auto-triggered by 7d window). -->
<!-- Built for X, Grok & the ecosystem community. -->

# Sample 3 output — 7-day window (Mention Audit auto-triggered; near-paradox watch)

> Generated by running `mention-summarizer/run.py` against `sample-3-input.json` (7d window demo mentions for `@JanSol0s`).

| Field | Value |
|---|---|
| Creator handle | `@JanSol0s` |
| Window | `7d` |
| Comparison basis | `previous_period` |
| Data source | seeded demo mentions (re-run with --mentions-file pointing at the actual X mention export for real data) |
| Sentiment-spike paradox? | **No** — Mention volume delta +29.2% (just below the +30% threshold; near-paradox watch fires instead) |
| Troll cluster? | **No** — no coordinated negative cluster detected |
| Mention Audit triggered by? | 7-day window (single-day variance exposure caveat) |

To regenerate:

```powershell
python .\run.py `
  --handle JanSol0s `
  --mentions-file .\examples\sample-3-input.json `
  --days 7 `
  --compare-to previous_period `
  --no-banner
```

---

## Mention Snapshot
**@JanSol0s: 7d mention window — directional period (Mention Health 52/100); monitor sentiment trend.**

- **Creator handle**: @JanSol0s
- **Window**: 7d
- **Comparison basis**: previous_period
- **Data source**: seeded demo mentions — re-run with --mentions-file for real X data

## Mention Health

| Metric | Value | Trend | Interpretation |
|---|---|---|---|
| Mention volume delta | +29.2% [demo mention — re-run with --mentions-file for real X data] | ▲▲ | Rising; the trend supports the current content cadence. |
| Net sentiment score  | 3.2 [demo mention — re-run with --mentions-file for real X data] | ▬ | Near-neutral; the period's mentions split roughly evenly across sentiment bands. |
| Priority reply rate  | 12.9% [demo mention — re-run with --mentions-file for real X data] | ▬ | Moderate reply burden; manageable with a structured daily triage session. |
| Authentic reach share| 87.1% [demo mention — re-run with --mentions-file for real X data] | ▬ | Majority of mentions from established accounts; bot-floor signal stable. |

**Mention Health score**: 52/100

## Sentiment Breakdown

| Band     | Count | Share  |
|---|---|---|
| Positive | 9 [demo mention — re-run with --mentions-file for real X data] | 29.0% |
| Neutral  | 14 [demo mention — re-run with --mentions-file for real X data]  | 45.2% |
| Negative | 8 [demo mention — re-run with --mentions-file for real X data] | 25.8% |

Sentiment is split across bands; no single tone dominates the period.

## Priority Reply Queue (4 priority mentions)

1. **Niche peer sharing a new benchmark result that directly extends the top thread's central argument** (positive · priority 89 [demo mention — re-run with --mentions-file for real X data]) — bridges to: `reply-drafter`
2. **Developer asking whether the offline runner design applies to edge-compute deployments** (neutral · priority 76 [demo mention — re-run with --mentions-file for real X data]) — bridges to: `reply-drafter`
3. **Creator in an adjacent niche asking to collaborate on a thread that bridges both communities** (positive · priority 64 [demo mention — re-run with --mentions-file for real X data]) — bridges to: `reply-drafter`
4. **Question about the CLI runner's PowerShell compatibility on Windows Server environments** (neutral · priority 55 [demo mention — re-run with --mentions-file for real X data]) — bridges to: `reply-drafter`

## Red Flags

- **Single-day variance exposure** · severity: medium — A 7d mention window is dominated by single-day variance; one viral post or one quiet day can flip every trend arrow. *Remediation:* Re-run with --window 30 once 14+ more days have passed before declaring directional reads.
- **Near-paradox watch** · severity: low — Mention volume delta at +29.2% is growing while Net sentiment score at 3.2 remains modest — one more negative-heavy window could trigger the sentiment-spike paradox. *Remediation:* Run `analytics-summarizer` to confirm whether the content driving volume is the same content earning engagement — divergence here is an early-warning signal.

## Recommendations

1. Work through the 4-mention priority queue in sequence; voice-faithful replies only, no engagement bait. — bridges to: `reply-drafter`
2. Cross-reference this mention window against the period analytics to confirm the sentiment trend is consistent with the broader engagement signal. — bridges to: `analytics-summarizer`
3. Audit whether the content archetype that drove the mention spike attracts the right audience — divergent niches produce volume without community sentiment. — bridges to: `follower-quality-analyzer`
4. Compare the sentiment pattern against competitor mention windows to confirm the signal is specific to this creator's content, not a niche-wide trend. — bridges to: `competitor-watch`
5. Promote the content archetype that drove the mention spike into a structured A/B to confirm the format lifts engagement rate, not just volume. — bridges to: `ab-test-suggester`

## Confidence
Confidence: low — data source is seeded demo mentions — re-run with --mentions-file pointing at the actual X mention export to lift confidence.

## Mention Audit (auto-triggered)

- **Window adequacy**: 7d window is dominated by single-day variance; treat all reads as directional.
- **Data source confidence**: seeded demo placeholders — runner cannot speak to real-creator confidence until --mentions-file is supplied.
- **Troll-cluster impact**: none detected — negative sentiment band reflects genuine independent mentions.
- **Suggested next sample**: re-run when 14+ more days have passed; widen to 30d if currently on 7d.
- **Re-run cadence**: weekly during active mention spikes, otherwise monthly.
